{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "cmake",
			"label": "CMake: build",
			"command": "build",
			// "targets": ["ALL_BUILD"], // Use the "CMake: Set build target" command instead
			"group": {
				"kind": "build",
				"isDefault": true,
			},
			"problemMatcher": ["$msCompile", "$msvc"],
			"detail": "CMake template build task",
		}
	]
}